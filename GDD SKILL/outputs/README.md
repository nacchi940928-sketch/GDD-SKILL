# outputs/（已废弃）

> **2026-07-07 起**：编译三包不再写入本目录。

`compile.py` 默认输出改为 **`{feature_root}/开发文档/`**，与 01~04、logs 同属一份策划案：

| 类型 | 路径 |
|------|------|
| 管线拆解（02/03/04） | `产出/{功能名}/02-03需求拆解与功能点梳理/` + `04/` |
| 编译整合（三包） | `产出/{功能名}/开发文档/` |
| 对照样例 | `案例/{功能名}/` + `案例/{功能名}/开发文档/` |

改 02/03 后重新运行：

```bash
python tools/compile.py 产出/{功能名} --workflow workflows/{项目}.workflow.json
```

本目录仅保留 `.gitkeep`，便于旧链接跳转说明。
