# 02-需求拆解 — 写作规范

> **读者**：策划、运营、产品、管理层。  
> **不是**：程序实现规格（那是 `03-功能点梳理.md`）。

---

## 02 vs 03

| | 02 需求拆解.md | 03 功能点梳理.md |
|--|----------------|------------------|
| 语言 | **自然语言**，像策划案扩写 | 技术规格，可交给程序/AI |
| 规则 | 何时、对谁、发生什么、为什么 | R-xx **伪代码**、检测位置、P-xx 读写 |
| 流程 | 玩家/界面视角的阶段描述 | Phase 枚举、状态机、协议时序 |
| 数据 | 需要哪些表/配置（**表名 + 代表字段**） | 附录 A 全字段、配置表、协议、审计 |
| 编号 | R-/EX-/T- 作**对照锚点**；配置项写 **P-xx + `表.字段`** | P-xx 全文、附录 A |
| 禁止 | 伪代码、协议表、Const 审计、数据契约 | 「见 02」「见策划案」 |

---

## 规则条目格式（02）

```markdown
## R-{缩写}-001 {规则标题}

**边界**：继承现网 · {模块名}（仅写 delta 时说明改动点）
<!-- 或：本次新增 / 本次改动（一句话说明相对现网差异） -->

**规则说明**：（2~5 句自然语言：触发情境 → 系统行为 → 玩家可见结果 → 例外）

【待确认】…（如有）

**来源**：01 §x.x
```

**边界**取值：`继承现网` · `本次新增` · `本次改动`（与 01 边界标注一致，落到每条规则）。

**不要写**：`检测位置`、`检测方式`、代码块、`| 读/写 | P-xx |` 表。

竞争、排序、计时类规则须在「规则说明」中写清 **终裁方**（由谁判定结果）；01 有歧义时须择一，不得同时保留「轮流」与「同时」。

---

## §1.x 范围：做 / 不做（必写）

在 **§1 功能概述** 中增加范围表：

```markdown
### 1.x 范围：做 / 不做

| 做（本包范围） | 不做（MVP 或非目标） |
|----------------|----------------------|
| 从 01 提取     | 从 01 提取；无则【待确认】 |
```

无明确「不做」时仍须建表，第二列填【待确认】，禁止留空。

---

## 状态机（02）

用**玩家能感知的阶段名**：

```markdown
### 2.1 单局流程

选角 → 匹配中 → 对局内（每轮：抢道具 → 放道具 → 跑酷 → 小结算）→ 总结算 →（可选）加赛
```

**不要写**：`LOBBY`、`P-17`、`MatchStartReq`（放 03）。

### §2.x 阶段内状态（每条子流程必写）

每个阶段除名称外，须一行说明：

- 该阶段内玩家能否移动、是否忽略重力等特殊物理
- 倒计时是否**必须等满**（全员提前完成是否仍等待）
- 能否放弃/跳过/主动丢弃操作
- 失败/超时如何处置 → 引用 R-{缩写}-xxx

示例：

```markdown
- **放置阶段**：四人在网格内自由移动（忽略重力）；须等满倒计时才进入跑酷；无主动丢弃 → 见 R-XXX-009
```

不得在同一 §2 中既写「轮流」又写「同时」；若 01 有歧义，按 §3 已定 R- 统一表述。

---

## 系统与内容分解（02 · §1.5）

在 **§1 功能概述** 之后、**§2 流程** 之前，按业务系统列出**内容子类**（策划语义索引，不是规则正文）。

```markdown
## 1.5 系统与内容分解

### SYS-CHH-001 布置系统

**边界**：本次新增

| 编号 | 内容子类 | 策划定义 | 数量级 | 配置/数据来源 | 关联规则（R-） | 关联 UI | 边界 |
|------|----------|----------|--------|---------------|----------------|---------|------|
| CNT-CHH-001 | 可放置道具 | 玩家从木箱选取并放到地图的机关与平台 | ~20 种 | 道具表（§6） | R-CHH-010~014 | 布置 HUD | 本次新增 |
| CNT-CHH-002 | 蜂蜜连接 | 部分道具间的物理粘合关系 | — | Const + 道具字段 | R-CHH-014 | — | 本次新增 |

**代表例**（可选）：弹簧板、冰面、传送门…
```

**编号**：`SYS-{缩写}-{序号}` 系统；`CNT-{缩写}-{序号}` 内容子类。

**§1.5 写什么**：子类名称、策划定义、数量级、对应 §6 表名、预留 R-/UI、边界。

**§1.5 不要写**：伪代码、P-xx 列、逐行配置、完整实体清单（代表例 1~3 个即可）。

**可跳过**：纯流程/入口改版且无新内容子类 → 一节说明「本包无新增 CNT」，指向 §3/§6。

§3 规则可写：`适用于 CNT-CHH-001`。

---

## 数据章节（02）

```markdown
## 6. 数据概要

本玩法依赖以下配置（名称与 01 一致，**须带程序字段名**）：
- **AB2_XxxConst**：单局轮数 `total_rounds`、各阶段倒计时…（对应 P-01、P-03…）
- **AB2_XxxItem**：道具行为与权重…
- **AB2_XxxScore**：成就分…

程序可读 P-xx 与完整字段表见 **03-功能点梳理.md 附录 A**。
```

---

## 验收（02）

保留 **T-/B-/E-** 编号，步骤与预期用**用户操作语言**（「点击匹配」「全员到达终点」），字段编号可省略或括号注明。

---

## 需求拆解 Agent 自检

- [ ] 策划同事能否**不打开 03** 即理解玩法与规则？
- [ ] 02 中是否仍含伪代码 / 协议表 / P-xx 大表 / 数据契约审计？→ 迁到 03
- [ ] 每条 R- 是否有「规则说明」自然段？
- [ ] 核心 R- 是否标注 **边界**（继承现网 / 新增 / 改动）？
- [ ] §1.5 各系统**子类**是否齐全（或已注明无新增 CNT）？
- [ ] §1 是否含 **做/不做** 范围表？
- [ ] §2 各阶段是否含 **阶段内状态**？
- [ ] 竞争/排序类 R- 是否已写 **终裁方**？
